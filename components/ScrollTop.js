import '../scss/scroll-top.scss';

const ScrollTop = () => {
  return (
    <button class='scroll-top-btn hidden'>
      <i class='far fa-hand-pointer'></i>
    </button>
  );
};

export default ScrollTop;
